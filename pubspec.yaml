name: healing_budget
description: A healing budget app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0        # ← 必须添加这一行！
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1      # ← 用于生成 .g.dart

flutter:
  uses-material-design: true
  # assets:   ← 整行注释掉或删除
