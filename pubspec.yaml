name: healing_budget
description: 治愈系记账
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  speech_to_text: ^6.5.0
  permission_handler: ^11.3.1

dev_dependencies:
  hive_generator: ^2.0.0
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
